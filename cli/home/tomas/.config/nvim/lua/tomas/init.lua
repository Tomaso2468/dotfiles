require("tomas/set")
require("tomas/packer")
require("tomas/remap")

vim.filetype.add({
    extension = {
        frag = "glsl",
        vert = "glsl",
        geom = "glsl",
        fsh = "glsl",
        vsh = "glsl",
        gsh = "glsl",
    }
})
